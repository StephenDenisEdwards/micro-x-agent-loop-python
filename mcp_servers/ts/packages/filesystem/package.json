{
  "name": "@micro-x-ai/mcp-filesystem",
  "version": "0.1.0",
  "description": "Filesystem MCP server (bash, read_file, write_file, append_file, save_memory, grep, glob)",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-filesystem": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@micro-x-ai/mcp-shared": "*",
    "@vscode/ripgrep": "^1.15.9",
    "fast-glob": "^3.3.2",
    "mammoth": "^1.8.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
